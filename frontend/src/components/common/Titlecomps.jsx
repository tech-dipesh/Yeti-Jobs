export default function Titlecomps({text}) {
  return (
    <div  className='text-5xl font-extrabold tracking-widest drop-shadow-lg bg-linear-to-r from-cyan-400 to-blue-500 bg-clip-text text-transparent'>{text}</div>
  )
}
