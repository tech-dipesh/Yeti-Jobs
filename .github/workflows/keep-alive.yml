name: Keep Render Alive
on:
  schedule:
    - cron: "*/5 * * * *"
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl https://yeti-jobs.onrender.com/api/v1/health
